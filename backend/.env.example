# Backend Configuration
PORT=5000
MONGODB_URI=mongodb://localhost:27017/piverse

# Solana Configuration (Ignored in Off-Chain Mode but good to have)
# RPC_URL=https://api.devnet.solana.com
# WALLET_PRIVATE_KEY=[]

# AI Configuration (Primary: Groq, Fallback: Gemini)
GROQ_API_KEY=your_groq_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# ----------------------------------------------------
# OFF-CHAIN / CAPTURE THE FLAG EVENT CONFIGURATION
# ----------------------------------------------------

# The Secret Seed Phrase to reveal to the winner
PRIZE_SEED_PHRASE="replace this with your twelve word secret seed phrase here immediately"

# The visual Jackpot amount (in SOL) to display on the frontend
INITIAL_JACKPOT=100

# Minimum global attempts before AI can yield (e.g. 500)
MIN_ATTEMPTS=500
